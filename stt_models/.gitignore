# Dictation captures are recordings of your own voice plus their transcripts — local
# eval data, not something to push to a remote.
captures/
captures.jsonl
__pycache__/

# Per-machine state, not repo defaults: which model you last picked, and your own
# pronunciation list. vocab.example.txt is the tracked starting point for both.
model.txt
vocab.txt
model.txt.tmp
vocab.txt.tmp
